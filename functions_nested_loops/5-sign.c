#ifndef HEADER_FILE
#define HEADER_FILE
#include "main.h"
#endif

/**
 * print_sign - asdfg
 * @n: asdf
 * 
 * Return: asdfg
 */
int print_sign(int n)
{
	if (n > 0)
		_putchar('+');
		return (1);
	else if (n < 0)
		_putchar('+');
		return (-1);
	else
		_putchar('+');
		return (0);
}
