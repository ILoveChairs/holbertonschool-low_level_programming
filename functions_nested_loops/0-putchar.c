#include "main.h"

/**
 * main - asdfg
 *
 * Description: asdfg
 * Return: 0 motivation
 */
int main(void)
{
	int i;
	char out[] = "_putchar\n";
	int length;

	length = sizeof(out) / sizeof(out[0]);;
	for (i = 0; i < length; i++)
		_putchar(out[i]);
	return (0);
}
