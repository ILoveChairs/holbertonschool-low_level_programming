#ifndef HEADER_FILE
#define HEADER_FILE
#include "main.h"
#endif

/**
 * create_array - asdfg
 * @size: asdfg
 * @c: asdfg
 * Return: asdfg
 */
char *create_array(unsigned int size, char c)
{
	char *s;

	if (size == 0)
		return (0);

	s = malloc();
	return (s);
}
